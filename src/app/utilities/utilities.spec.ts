/*
import {dateToString, stringToDate} from "./utilities";

describe('Utility function dateToString', () => {
  it('should return "2017-03-15" from Date(2017, 2, 15)', () => {
    const result = dateToString(new Date(2017, 2, 15));
    expect(result).toEqual('2017-03-15');
  });

  it('should return "" from undefined', () => {
    const result = dateToString(undefined);
    expect(result).toEqual('');
  });
});

describe('Utility function stringToDate', () => {
  it('should return Date(2017, 2, 15) from  "2017-03-15"', () => {
    const result = stringToDate('2017-03-15');
    expect(result).toEqual(new Date(2017, 2, 15));
  });

  it('should return Date(2017, 2, 15) from "abcdefghij', () => {
    const result = stringToDate('abcdefghij');
    expect(result).toEqual(new Date(1970, 0, 1));
  });
});
*/
