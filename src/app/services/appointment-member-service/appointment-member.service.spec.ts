/*
/!* tslint:disable:no-unused-variable *!/

import { TestBed, async, inject } from '@angular/core/testing';
import { AppointmentMemberService } from './appointment-member.service';

xdescribe('Service: AppointmentMember', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AppointmentMemberService]
    });
  });

  it('should ...', inject([AppointmentMemberService], (service: AppointmentMemberService) => {
    expect(service).toBeTruthy();
  }));
});
*/
