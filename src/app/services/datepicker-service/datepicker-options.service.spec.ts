/*
/!* tslint:disable:no-unused-variable *!/

import { TestBed, async, inject } from '@angular/core/testing';
import { DatepickerOptionsService } from './datepicker-options.service';

describe('DatepickerOptionsService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DatepickerOptionsService]
    });
  });

  it('should ...', inject([DatepickerOptionsService], (service: DatepickerOptionsService) => {
    expect(service).toBeTruthy();
  }));
});
*/
